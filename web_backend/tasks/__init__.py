"""Web 持久化任务的桥接执行、动作编排、结果投影与后台状态机。

请求经 Handler 创建持久化任务后，``runner`` 在后台线程驱动状态流转；``actions`` 组合
Web 复核动作与 Core 桥接动作；``bridge`` 为每个任务启动独立 Python 子进程并消费
stderr 事件；``results`` 负责发现输出文件、隐藏服务端绝对路径并生成受控下载 URL。
所有输出、缓存和运行目录都按用户 ID 隔离。
"""

