"""Web 业务领域服务包。

本包承载 HTTP 路由之外的 Web 领域服务：账号与认证、工作台聚合、管理员数据、
日清人员/事项/资料维护、日清报告快照、备份恢复等。服务函数统一通过依赖对象
接收数据库连接、锁、路径和 Core 模块，不直接导入 ``web_server``，使测试可以
注入临时数据库，部署配置也能在运行时绑定。

模块边界：
- auth / admin_accounts / admin_data：账号生命周期、管理员审核与审计查询；
- dashboard：工作台指标、趋势与通知聚合；
- backups：备份创建、校验、下载与恢复；
- daily_*：日清领域拆分实现与兼容门面。
"""

